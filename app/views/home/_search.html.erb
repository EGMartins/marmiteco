<% unless session[:id].present? %>
  <div class="container">
    <section id="search">
      <%= form_tag map_path, :method => :get do %>
    <div class="row">
      <div class="input-field col s12">
        <i class="material-icons prefix">location_on</i>
        <%= label :search, 'Coloque um endereço para encontrar comida' %>
        <%= text_field_tag :search, params[:search] %>
      </div>
      <div class="col s12 valign">
          <%= submit_tag "Por perto", :name => nil, class: "waves-effect waves-orange btn" %>
      </div>
    </div>
  <% end %>
    </section>
  </div>
<% end %>
